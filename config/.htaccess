# Block direct web access to config helper files.
# The admin settings page (index.php) is explicitly re-allowed below.
<FilesMatch "\.php$">
  Require all denied
</FilesMatch>

<Files "index.php">
  Require all granted
</Files>
