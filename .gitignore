# Local configuration overrides (contains credentials — never commit)
config/local.php

# vendor/ and composer.lock are intentionally committed so the app runs on servers
# without requiring a separate composer install step (no CI/CD build pipeline).

# Upload artifacts
uploads/*
!uploads/.gitkeep
!uploads/.htaccess

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
