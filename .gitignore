# Local configuration overrides (contains credentials — never commit)
config/local.php

# Composer dependencies
vendor/
composer.lock

# Upload artifacts
uploads/*
!uploads/.gitkeep
!uploads/.htaccess

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
